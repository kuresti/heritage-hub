import { PersonFilterPipePipe } from './person-filter.pipe';

describe('PersonFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new PersonFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
